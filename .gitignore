# node modules
node_modules
.pnpm-store/
# build path
dist/*
# environmental variables
/.env
.env
.env.local
# yarn error log
yarn-error.log
# vscode config
.vscode
#coverage
coverage
src/utils/.DS_Store
src/services/.DS_Store
.DS_Store

# Claude local settings (user-specific)
.claude/

/certs/

frontend/.env
smart-contracts/registry/wallet1.addr
smart-contracts/registry/wallet1.sk
.env.*
!.env.example
smart-contracts/payment/wallet_2_.sk
smart-contracts/payment/wallet_2_.addr

# Mock data scripts (local dev only)
scripts/add-mock-payments.ts

otelcol-contrib
# invoice-generator test artifacts
test-output/
tests/e2e/.e2e-global-state.json
scripts/example-invoice.pdf
scripts/example-invoice.html

# Prisma Generated Files
src/generated/

# Local agent/tooling scratch databases (ruvector / agentdb, .remember)
*.rvf
*.rvf.lock
ruvector.db
**/ruvector.db
.remember/

# Hydra L2 E2E harness — generated seller mnemonic (never commit)
hydra-l2-flow/.seller.json

# Hydra native run (large binaries + runtime state)
hydra-l2-flow/.bin/
hydra-l2-flow/.native-state/

# Hydra preprod run (local binaries, secrets, logs + runtime state)
hydra-l2-flow/preprod/bin/
hydra-l2-flow/preprod/cardano-data/
hydra-l2-flow/preprod/credentials/
hydra-l2-flow/preprod/downloads/
hydra-l2-flow/preprod/ipc/
hydra-l2-flow/preprod/logs/
hydra-l2-flow/preprod/persistence/
hydra-l2-flow/preprod/*.pid
hydra-l2-flow/preprod/protocol-parameters*.json

# Hydra L2 E2E — generated evidence reports (regenerate via run-hydra-e2e.sh evidence)
hydra-l2-flow/evidence/
