// Re-export everything from the npm package so existing imports continue to work.
// The package is the single source of truth — update it there, not here.
export * from 'masumi-schema-validator-component';
